version: 1
default:
  allow_dns: true
  domains:
    - { host: api.anthropic.com, ports: [443] }
    - { host: api.openai.com,   ports: [443] }
  cidrs: []
per_task_widening:
  requires_approval: true
